def findDuplicate(arr, n):
    for i in range(n):
        for j in range(i + 1, n):
            if arr[i] == arr[j]:
                return arr[i]

arr = [1, 3, 4, 2, 2]
n = len(arr)

print("Duplicate element:", findDuplicate(arr, n))